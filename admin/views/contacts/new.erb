<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:contacts, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:contacts, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :contact, url(:contacts, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'contacts/form', :locals => { :f => f } %>
 <% end %>
</div>
